using System;
using System.Collections.Generic;
using System.Text;

namespace RelaScript
{
    public class LibraryFunctionPointer
    {
        /*private Func<object, object> CompiledFunc = null;

        public object Call(string libname, string funcname, object args, InputContext context)
        {
            if(CompiledFunc == null)
            {
                Compile(libname, funcname, context);
            }
        }

        private void Compile(string libname, string funcname, InputContext context)
        {
            Expression total = 
        }*/
    }
}
